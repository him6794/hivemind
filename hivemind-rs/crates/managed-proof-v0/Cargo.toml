[package]
name = "hivemind-managed-proof"
version = "0.1.0"
edition = "2021"
license = "MIT"

[features]
default = []
risc0-verifier = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "1"

[workspace]
