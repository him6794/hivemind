# Hivemind E2E State

- Goal: achieve production-grade reliability DoD for the real distributed pipeline.
- Success criteria for this round: add a repeatable reliability harness and use it to gather real evidence for the next DoD gap without claiming unproven success.
- Status: running
- Current step: full DoD validation is running in the background.
- Completed:
  - Prior worker/nodepool package-level test regressions are already fixed and verified.
  - Existing findings/TODO/architecture notes have been read and are current up to iteration 7.
  - Resume check confirmed existing host containers occupy 15432/16379/8082; this run will avoid them.
  - Iteration 8 reproduced duplicate redispatch in a live run, fixed `UploadTask`, and verified duplicate rejection in unit and live E2E.
  - Live run `20260520074442` completed 5 labeled tasks, rejected duplicate submission, marked crashed worker2 `OFFLINE`, and restored worker2 to `ACTIVE` after reconnect.
  - Commit `afa13c1` records the iteration 8 root cause fix.
  - Reliability harness calibration run `rel-20260520082011-r01` completed one startup/shutdown cycle with latency proxies and crash/reconnect events.
  - Latest generated reports show DoD is not satisfied: all workload tasks stayed `PENDING [NO_WORKER]` because workers were not registered in nodepool.
  - Isolation run `rel-20260520083101-r01` proved worker registration can succeed when workers register directly to nodepool, and exposed `[PROBE_FAIL]` on nodepool-to-worker dispatch under latency.
  - Iteration 9 unit evidence reproduced the latency-probe failure and fixed nodepool's pre-dispatch probe timeout.
  - Nodepool package tests pass after iteration 9.
  - Calibration run `rel-20260520084038-r01` no longer reports `[NO_WORKER]` or `[PROBE_FAIL]`; it now fails earlier at master upload HTTP 502 caused by a 5s gRPC deadline.
  - Iteration 10 reproduced the hard-coded master timeout with a RED test and changed master nodepool RPC timeout to a configurable 30s default.
  - Master package tests pass after iteration 10.
  - Calibration run `rel-20260520084623-r01` no longer reports master HTTP 502; uploads return HTTP 200 after about 15-16 seconds.
  - Iteration 11 reproduced the DelayProxy idle accept-loop bug with a RED Python unittest and fixed timeout handling.
  - Calibration run `rel-20260520085710-r01` proves worker proxy connections are now active under latency.
  - Iteration 12 reproduced settlement refreshing `LastUpdate` with a RED nodepool test and stopped normal settlement ticks from masking task timeout.
  - Calibration run `rel-20260520090714-r01` passed with 3 workers, latency/jitter, duplicate rejection, one worker kill/reconnect, failure-injected task, retry task, long task, and parallel workload.
  - Calibration run `rel-20260520091126-r01` also passed with the default proxied worker-to-nodepool path (`worker_nodepool_addr=127.0.0.1:55051`).
  - Full DoD validation was started as background PID `17316` with `--runs 10 --failure-simulations 3 --long-seconds 900 --stop-on-failure`.
  - Full DoD artifacts are being written under `test_logs/reliability/20260520-091358`.
- Next action:
  - Monitor PID `17316` and inspect `test_logs/reliability/20260520-091358/summary.json` when available.
  - If the full run fails, record the first failure as the next single-root-cause iteration.
- Checkpoint:
  - Iteration 8 is committed. Reliability harness work is in progress and must not be counted as DoD completion.
  - Only a calibration run has executed; full 10-run DoD validation has not passed.
- Blockers:
  - Existing host stack owns 15432, 16379, and 8082; continue using alternate isolated ports for E2E runs.
  - Full DoD still needs 10 consecutive full pipeline runs, 3 consecutive node failure simulations, true network delay, and real failure/long-running workload behavior.
  - Full DoD result is pending; do not mark the goal complete until the background run proves all gates.
- Owners:
  - Coordinator: Codex
  - Runtime: local shell processes and Docker containers
