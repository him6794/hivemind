# HiveMind Batch Distributed Compute Runtime 架構完善計畫 v2 

## Summary

目前系統已具備 `master -> nodepool -> worker` 雛形與可靠性驗證基礎，但下一階段要明確收斂為：

- 面向公網、高延遲、不穩定節點的 `Batch Distributed Compute Runtime`
- 低通訊頻率、粗粒度任務、pull-based batch execution
- 中心化協調，但避免 nodepool 維護 heavy worker state
- Python 僅作 DSL，Rust Worker Runtime 作為最終唯一 execution runtime
- Artifact Store 是核心子系統，不是輔助工具

明確禁止演化方向：

- fine-grained RPC orchestration
- actor-based runtime
- distributed shared memory
- micro-task execution
- DNS-based task routing
- Python runtime creep

## Key Interfaces

- DAG IR：
  - 包含 `job_id`、`nodes`、`edges`、`resource_requirements`、`artifact_inputs`
  - 新增 `max_retries`、`deadline`、`deterministic`、`side_effects`、`priority`
  - `deterministic=true` 且 `side_effects=false` 才允許 aggressive cache reuse、duplicate execution、speculative retry

- ExecutionPackage：
  - 位於 DAG IR 與 worker execution 之間
  - 包含 `runtime_version`、`task_code_ref`、`artifact_refs`、`constraints`
  - 用於 runtime compatibility、version pinning、reproducibility、cache reuse

- ArtifactManifest：
  - 每個 artifact 必須有 manifest layer，不只是一個 SHA256 blob
  - 包含 `artifact_id`、`chunks`、`size`、`compression`、`content_type`、`created_by_task`
  - 支援 chunk streaming、prefetch、compression negotiation、partial download、integrity check

- PullBatchRequest：
  - worker pull 任務時即時上報 capacity，不依賴 nodepool 長期保存 heavy worker state
  - 包含 `max_inflight_batches`、`available_memory`、`queue_capacity`
  - 包含 cache summary，不包含完整 cache manifest：`cache_epoch`、Bloom filter、partial cache digest、summary counters

- PullBatchResponse：
  - 包含 `batch_id`、task leases、artifact manifests/refs、resource limits、deadline
  - scheduler 根據 worker 即時 capacity 與 task priority 控制 batch 大小

- CompleteBatchRequest：
  - 包含每個 task 的 status、stdout/stderr refs、result artifact refs
  - 新增 execution metrics：`cpu_time`、`wall_time`、`peak_memory`、`download_bytes`、`cache_hits`
  - metrics 用於後續 scheduler optimization 與 capacity estimation

## Implementation Changes

- Nodepool / Scheduler：
  - 僅保存 leases、retry state、task ownership、minimal worker liveness。
  - 不保存完整 worker cache state。
  - locality scheduling 只使用 PullBatch 當下提供的 Bloom filter/cache summary 做即時計算。
  - 實作 backpressure：當 worker queue 滿、memory 不足、inflight batch 達上限時不再派發。
  - 保留弱狀態設計，避免 scheduler CPU/memory 隨 artifact 數量爆炸。

- Worker Runtime：
  - worker 負責本地 artifact cache、local queue、batch engine、resource manager。
  - worker 每次 PullBatch 回報即時 capacity 與 cache summary。
  - worker 不做 scheduling、不解析 DAG、不做 task routing decision。
  - artifact download 卡住或 local queue 滿時，worker 必須停止或降低 PullBatch demand。

- Artifact Store：
  - 作為核心子系統優先設計。
  - 必須支援 chunk integrity、corruption recovery、resumable transfer、deduplication、cache consistency。
  - SHA256 CAS 只是底層 storage primitive，所有上層傳輸與執行都依賴 ArtifactManifest。
  - torrent/magnet 可作 legacy ingestion/transport backend，但不作 task routing 或 artifact identity 的唯一來源。

- Python DSL / Compiler：
  - DSL 僅允許 task declaration、DAG description、resource annotations。
  - 禁止 runtime logic、mutable global state、networking、scheduler interaction、dynamic execution flow。
  - Compiler 只產生 DAG IR 與 ExecutionPackage metadata。
  - Compiler 不執行 Python、不連接 nodepool、不做 scheduling。

- Network：
  - VPN overlay 用於節點連線。
  - DNS 僅做 node discovery。
  - task routing 僅由 nodepool scheduler lease 決定。
  - worker 採 pull-based low-frequency communication，單次 request 取得 batch。

## Test Plan

- DAG IR tests：
  - 驗證 `deterministic` / `side_effects` 對 cache reuse、retry、duplicate execution policy 的限制。
  - 驗證 `max_retries`、`deadline`、`priority` 會進入 scheduler decision。

- Batch scheduler tests：
  - worker `queue_capacity=0` 不派發 batch。
  - worker `available_memory` 不足不派發超額 task。
  - `max_inflight_batches` 達上限時不再派發。
  - Bloom filter cache hit 只影響 locality score，不寫入 nodepool cache index。

- Artifact Store tests：
  - chunk integrity failure 可偵測並重傳。
  - interrupted transfer 可 resume。
  - duplicate artifact 可 dedup。
  - manifest compression/content type 支援 negotiation。
  - corrupted local worker cache 不污染 CAS truth。

- Worker runtime tests：
  - artifact download 卡住時不繼續拉新 batch。
  - local queue 滿時 PullBatch demand 降低。
  - batch 內單 task failure 不阻斷其他 task completion report。
  - execution metrics 正確回傳。

- Regression gates：
  - 保留現有 10-run reliability gate、3 node failure simulations、network delay/jitter、15-minute long-running workload。
  - 新增 batch gate：單次 network call 必須可執行多個 tasks。
  - 新增 no-heavy-state gate：nodepool 不保存完整 worker cache manifest。
  - 新增 no-RPC-runtime gate：不可新增 actor、distributed object call、fine-grained runtime RPC。

## Assumptions

- 第一版不做 Ray-like actor runtime。
- 第一版不綁定加密貨幣，credit/accounting 僅作平台內部功能。
- 完整 sandbox isolation 以 Linux/cgroups/container 優先，跨平台 worker 先保留能力分級。
- Go worker 與現有 `UploadTask/ExecuteTask` 作 legacy compatibility path，逐步遷移到 Rust worker + PullBatch。
- Artifact Store 優先級高於 DSL polish，因為 storage contract 會決定 scheduler、worker cache、retry 與結果一致性。