# Managed Proof Guest Build Attestation

Date: 2026-08-11
Base commit: `01ffb3a944f42205426a8b8d0ee6e79a83296e63`

## Why this rebuild happened

The guest shares `hivemind-managed-proof` and `managed-function-runtime` with the
Worker. Two committed changes altered that shared source after the previous
attestation — `097c98a fix(runtime): enforce finite managed execution limits`
and `0158129 fix(runtime): bound canonical output and value materialization` —
so the previously trusted image ID, receipt fixture, and attestation were all
stale. Four of the nine tracked build inputs below changed hash.

Rebuilding produced a new guest image ID. The previous value was:

```text
[3606400121, 4250889949, 2277454476, 3430793801,
 2111044864, 2713379816, 851522248, 2751351423]
```

## Result

The RISC Zero methods build generated this guest image ID from the current
working source:

```text
[466412732, 2327327967, 2963073729, 178423767,
 1914766815, 1823038484, 4206432854, 2659673256]
```

The generated ID equals both
`hivemind_managed_proof_methods::HIVEMIND_MANAGED_PROOF_GUEST_ID` and the
Nodepool trust pin `hivemind_managed_proof::RISC0_MANAGED_GUEST_ID`. The tracked
regression test is `tests::generated_guest_id_matches_nodepool_trust_pin` in
`zkvm/managed-proof/host/src/lib.rs`.

Verification result:

```text
running 1 test
test tests::generated_guest_id_matches_nodepool_trust_pin ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 7 filtered out
mode=pin-test elapsed_seconds=114
cleanup_complete rc=0
```

The canonical test command is:

```text
cargo test --locked -p hivemind-managed-proof-zkvm \
  tests::generated_guest_id_matches_nodepool_trust_pin -- --exact
```

The local run used the repository's D:-backed WSL caches with
`HIVEMIND_ZKVM_USE_DOCKER=0`. After the command, neither
`/run/desktop/mnt/host/d` nor `/root/.cargo` remained mounted.

The trust pin was updated to the generated value and the build was then repeated
against that updated source. It still produced the same ID, confirming the pin's
own value does not feed back into guest codegen. Only the second run is cited
above as the passing result.

## Artifact digests

```text
a6707caf4c6968befa8f24ff9d9416a9bd6d09a858fd75e7b2be40079aeff036  guest ELF (527,936 bytes)
8221629b1ba7f2a22430cb4b18a8f2ecb02b306bedb1069d6290cbab95f890bb  risc0-managed-proof-v1.json (664,258 bytes)
```

The receipt fixture was regenerated by proving the golden vector against the
rebuilt guest (`mode=fixture elapsed_seconds=732`, `cleanup_complete rc=0`). Its
embedded `image_id` equals the trust pin above, which is what makes the fixture
usable as the positive verifier test.

Its shape is unchanged from the previous fixture: a 656-byte journal, one
Composite segment at index 0 hashed with poseidon2, no assumption receipts, and
a 63,914-word seal. Only the byte counts moved, and both remain far inside the
verifier's pre-crypto caps:

| Measure | Previous | Current | Cap |
|---|---:|---:|---|
| Envelope JSON | 664,026 | 664,258 | — |
| Receipt JSON | 661,720 | 661,953 | 2,097,152 (`RISC0_MAX_RECEIPT_JSON_BYTES`) |
| Journal | 656 | 656 | 4,096 (`RISC0_MAX_JOURNAL_BYTES`) |
| Segment seal words | 63,914 | 63,914 | 131,072 (`RISC0_MAX_SEGMENT_SEAL_WORDS`) |

`risc0_verifier_tests::real_fixture_stays_within_phase_one_resource_budget` pins
those measured values, so a future guest whose proofs grow toward a cap fails
the test rather than silently approaching it. The full verifier suite is 37
tests passing against this fixture, including the real cryptographic
verification in
`risc0_verifier_tests::verifier_accepts_pinned_real_receipt_and_returns_claim`.

The generated ELF path for this run was:

```text
D:\hivemind\.cache\zkvm-target\riscv-guest\hivemind-managed-proof-methods\hivemind-managed-proof-guest\riscv32im-risc0-zkvm-elf\release\hivemind-managed-proof-guest.bin
```

## Build-input digests

All values are SHA-256. Entries marked `changed` differ from the 2026-08-09
attestation and are what forced the rebuild.

```text
32b480079501507e1667897a5be7cfcc53e27937934fdc70126410da0c81c69e  hivemind-rs/crates/managed-proof/Cargo.toml
4d7f7bf524b45d57cdfed56b6abc3f31bd2d2219524d66dc42435d97baa9e595  hivemind-rs/crates/managed-proof/src/lib.rs            (changed)
7994b6576771a5cece6fd01cdf7a799fd55b0996b9ce43d1e912700e79d54e88  executor-rs/crates/managed-function-runtime/Cargo.toml
251bccabdd9d173bedf8dcbf2f772e9b5d734e98e982232310347b8bbe15b94c  executor-rs/crates/managed-function-runtime/src/lib.rs  (changed)
b256dee6c9c6487da6eebac6b8cbf68dc2f98454e21c78ff5fa90edca8d9897d  zkvm/managed-proof/Cargo.lock                           (changed)
5c04ef03b91d5c1d3de2cf95b34fd8fe22214a41c44a1a573894ea23e6d2acf8  zkvm/managed-proof/methods/Cargo.toml
5dafb569aafd6a523fed714722ff5df2bf8dbd551e9b06d76f6f8b7d5666ba2b  zkvm/managed-proof/methods/build.rs
9d646fcfd9c9e14204276534b8c4c3b4e13ef0258d17b6bd782d285544b841e5  zkvm/managed-proof/methods/guest/Cargo.toml
760ee5ed3e860e41e010b3b59b1085e0b3e8898b4d2728d3b46045e0b73fdd0c  zkvm/managed-proof/methods/guest/src/main.rs            (changed)
```

Note that `hivemind-rs/crates/managed-proof/src/lib.rs` is listed with its
post-update hash: it contains the trust pin, so its digest necessarily changes
whenever the pin does. The re-run described above is what establishes that this
is not circular.

## Staged prover sidecar

The release worker image bakes in a prebuilt sidecar from
`packaging/managed-prover/` (see `scripts/build-managed-prover.sh`). The staged
binary must embed this same guest image ID, or the Nodepool rejects every
envelope it produces.

### The image ID depends on the build environment, not only on the source

An earlier staged binary
(`c0dc79ea479b64af2d9f62f20315b4442d5776390d6091cf8a403ae22e67b983`, 95,640,904
bytes) was assumed to match because it was built from the same working tree.
A multi-container end-to-end run disproved that. Asked to prove the golden
vector, it returned:

```text
[851157164, 2331111488, 898154945, 2202623007,
 559143449, 4095204016, 1237502462, 1480841899]
```

which is not the trusted ID. It had been built in a container with its own
rzup-installed guest toolchain, while the pin was produced by the WSL native
path using this repository's cached toolchain. Same source, different guest.

The Nodepool behaved correctly throughout: both attempts were audited as
`event=rejected`, `reason="Managed proof verification failed"`, the task ended
`FAILED` with `billing_settled=false` and `managed_executed_ops=0`, and nothing
was settled from an unverified claim. The cost of the mismatch is availability,
never trust.

Two consequences, both now enforced rather than documented:

- `scripts/build-managed-prover.sh` runs
  `tests::generated_guest_id_matches_nodepool_trust_pin` **before** building and
  refuses to stage anything if this environment does not reproduce the pinned
  guest.
- "Built from the same source" is not evidence that a prover matches the pin.
  Only a build environment that reproduces the pinned image ID is.

This attestation records the local build evidence; the tracked pin-equality test
remains the executable guard for future source, dependency, or toolchain
changes.
